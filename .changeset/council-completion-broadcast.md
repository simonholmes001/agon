---
"@agon_agents/cli": minor
---

When "invoke council" is used in post-delivery Q&A, the CLI now automatically streams council progress and displays results without requiring manual interaction. An immediate acknowledgement is shown when the council starts, followed by a live-watch loop that surfaces each agent's analysis as it completes and terminates automatically on synthesizer completion.

---
"@agon_agents/cli": patch
---

Harden dependency installs by publishing `npm-shrinkwrap.json` with the CLI package and aligning repository lockfiles/workflows to the shrinkwrap-based lock source.

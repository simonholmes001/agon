---
"@agon_agents/cli": patch
---

Fix prompt input frame to resize dynamically in both directions, eliminating empty rows and preventing content above the frame from being overwritten when the frame grows.
